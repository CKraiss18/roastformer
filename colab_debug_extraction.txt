# DEBUG CELL - Add this right after your extraction code

import zipfile
import os

print("="*80)
print("DEBUG: CHECKING EXTRACTION")
print("="*80)

# Check current working directory
print(f"\nCurrent working directory: {os.getcwd()}")

# Check what's in /content/
print("\n📁 Contents of /content/:")
!ls -la /content/

# Check if preprocessed_data exists
print("\n📁 Looking for preprocessed_data:")
!ls -la /content/ | grep preprocessed

# List all extracted files
print("\n📦 Listing zip contents:")
zip_path = '/content/gdrive/MyDrive/Colab Notebooks/GEN_AI/roastformer_data_20251111_092727.zip'
with zipfile.ZipFile(zip_path, 'r') as zip_ref:
    file_list = zip_ref.namelist()
    print(f"Total files in zip: {len(file_list)}")
    print("\nFirst 20 files:")
    for f in file_list[:20]:
        print(f"  {f}")

# Try to find where preprocessed_data actually is
print("\n🔍 Searching for preprocessed_data:")
!find /content -name "preprocessed_data" -type d 2>/dev/null

print("\n🔍 Searching for train_profiles.json:")
!find /content -name "train_profiles.json" 2>/dev/null

print("="*80)
